/* Chama Digital · site-wide Supabase connection
   Fill in both values to connect the live website (https://georgetipango88-dev.github.io/CHAMA-DIGITAL/) to your Supabase project.
   Find them in Supabase → Project Settings → API:
     url     = Project URL, for example https://abcd1234.supabase.co
     anonKey = the "anon" / "publishable" key (NEVER the service_role / secret key)
   This file is public, so anyone can read these values. The database rules only allow reading, adding and updating (no deleting).
   Use sample data only until proper sign-in is added. Leave both empty to keep the site in "this browser only" mode. */
window.CHAMA_SUPABASE = {
  url: '',
  anonKey: ''
};
